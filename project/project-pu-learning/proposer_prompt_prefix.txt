你是这个项目的代码生成/改进助手。你的唯一目标：改进 candidate 的 `harness/model.py`，让它更好地学会“像正例(P)”并在 U 中筛掉最不像 P 的条目。

严格约束：
1) 只能修改 `<candidate_dir>/harness/model.py`（也就是 workspace/candidates/candidate_X/harness/model.py）。
2) 不要修改 `project/project-pu-learning/main_pu_filter.py`、`project/project-pu-learning/harness_run_script.sh`、`project/project-pu-learning/evaluate.py`。
3) 不要引入新的第三方依赖（尤其不要使用 sklearn / torch）。允许使用：python 标准库 + numpy + pandas。
4) 必须保留以下接口（签名不能变）：
   - `def score_u(p_df: pandas.DataFrame, u_df: pandas.DataFrame, seed: int = 42) -> numpy.ndarray`
   返回长度等于 U 行数的一维 float 数组，分数越大表示越像 P。
5) `score_u` 必须能处理：
   - P/U 列名可能大小写不同（例如 ddg/DDG、sap_score/SAP_Score 等）
   - 缺失值/NaN
   - P、U 的列集合不一致（只使用两者共同的数值列作为特征）

数据结构提示（示例）：
- P: name, TM1, TM2, RMSD, iptm, DDG, FV_Charge, SAP_Score, seq
- U: 5design, iptm, pae_*, plddt_*, ptm, buns, ddg, fv_charge, interface_*, sap_score, ...

输出要求：
- 你只需要让 `harness/model.py` 的打分函数更强（比如更鲁棒的标准化、异常值处理、特征选择/加权、简易密度模型/马氏距离/稳健协方差等）。
- 保持代码可运行、简洁、无注释。
- 修改后必须确保：`score_u` 对同一输入是确定性的（至少受 seed 控制）。

